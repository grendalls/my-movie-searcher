const initialState = {
  isLoading: false,
  data: [],
  totalPages: null,
  currentPage: null
};

export default (state = initialState, action) => {
  switch (action.type) {
    case 'FETCH_BEST_STARTED':
      break;

    default:
      break;
  }
};
